export interface CredentialStore { get(reference:string):Promise<string|undefined>; set(reference:string, secret:string):Promise<void>; delete(reference:string):Promise<void>; }
export class MemoryCredentialStore implements CredentialStore { private readonly values=new Map<string,string>(); async get(reference:string){return this.values.get(reference);} async set(reference:string,secret:string){this.values.set(reference,secret);} async delete(reference:string){this.values.delete(reference);} }
export function credentialReference(providerId:string):string { return `moderado/provider/${providerId}`; }
export async function resolveCredential(environment:string|undefined, reference:string|undefined, legacy:string|undefined, store:CredentialStore):Promise<string|undefined>{return environment??(reference?await store.get(reference):undefined)??legacy;}