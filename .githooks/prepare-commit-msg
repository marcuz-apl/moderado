#!/bin/sh
# The pre-commit hook updates VERSION and prefixes the subject before Git
# snapshots the index. Keeping this hook intentionally empty prevents a late
# VERSION update that Git would leave unstaged.
exit 0