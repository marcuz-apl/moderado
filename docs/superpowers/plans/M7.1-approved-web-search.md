# M7.1 approved web search Implementation Plan

> **For agentic workers:** Execute test-first, keep the change within existing workspace boundaries, and run the full offline suite before committing.

**Goal:** Provide a bounded, approval-gated web search tool without giving models unrestricted shell or network access.

**Architecture:** The tools package validates an HTTPS endpoint, query, result count, and response shape, then returns bounded search results and source metadata through the existing tool registry.

**Tech Stack:** TypeScript, Node.js standard library, Zod where external data is parsed, Vitest.

**Spec:** docs/superpowers/specs/M7.1-approved-web-search-design.md

## Global Constraints

- No new runtime dependency without owner approval.
- Keep provider, tool, and CLI boundaries intact.
- Automated tests remain offline and deterministic.
- Do not publish, upload, or contact a live provider during verification.

## Tasks

1. Add failing tool tests. 2. Implement validated native fetch and metadata. 3. Register the tool. 4. Run focused and full offline tests; document the approval boundary.

## Verification

Run 
pm.cmd test, 
pm.cmd run build, and git diff --check. Record any live-release action separately because it requires explicit owner approval.