# M7.4 provider expansion foundation Implementation Plan

> **For agentic workers:** Execute test-first, keep the change within existing workspace boundaries, and run the full offline suite before committing.

**Goal:** Make provider expansion reuse a shared OpenAI-compatible adapter while keeping the agent core provider-neutral.

**Architecture:** The providers package exposes a configured OpenAI-compatible adapter layered on the existing HTTP behavior. CLI connections select it through provider contracts without importing provider details into core.

**Tech Stack:** TypeScript, Node.js standard library, Zod where external data is parsed, Vitest.

**Spec:** docs/superpowers/specs/M7.4-provider-expansion-foundation-design.md

## Global Constraints

- No new runtime dependency without owner approval.
- Keep provider, tool, and CLI boundaries intact.
- Automated tests remain offline and deterministic.
- Do not publish, upload, or contact a live provider during verification.

## Tasks

1. Add failing adapter construction test. 2. Implement shared compatible configuration. 3. Keep existing NIM behavior intact. 4. Run provider and full offline validation.

## Verification

Run 
pm.cmd test, 
pm.cmd run build, and git diff --check. Record any live-release action separately because it requires explicit owner approval.