# Host Event Protocol Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans task-by-task.

**Goal:** Add a versioned host-event envelope for CLI and future desktop consumers.

**Architecture:** Contracts owns schemas; core owns sequencing and session identity; CLI adapts envelopes to its existing renderer. Existing raw event listeners remain compatible.

**Tech Stack:** TypeScript, Zod, Vitest.

**Spec:** `docs/superpowers/specs/M4.4-host-event-protocol-design.md`

### Task 1: Contracts

- [x] Add `HostEventEnvelopeSchema` with protocol version, session ID, sequence, timestamp, and `AgentEvent`.
- [x] Write failing contracts tests for accepted envelopes and rejected zero sequence or unsupported version.
- [x] Implement schemas and run the focused tests.

### Task 2: Core host stream

- [x] Write failing tests for stable session ID and monotonic sequences.
- [x] Implement `HostEventStream` in `packages/core` with `emit(event)`.
- [x] Add optional host listener to `AgentRunOptions`, preserving raw event listener behavior.
- [x] Run core focused tests.

### Task 3: CLI adapter

- [x] Write a renderer test that forwards `envelope.event` unchanged.
- [x] Adapt CLI chat composition to create one host stream per session.
- [x] Run CLI focused tests.

### Task 4: Documentation and validation

- [x] Document protocol version and desktop-host use in README and roadmap.
- [x] Run `npm test; npm run build; git diff --check`.
- [x] Commit with Alfazen versioning.
