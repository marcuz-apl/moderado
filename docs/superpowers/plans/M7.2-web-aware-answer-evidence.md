# M7.2 web aware answer evidence Implementation Plan

> **For agentic workers:** Execute test-first, keep the change within existing workspace boundaries, and run the full offline suite before committing.

**Goal:** Preserve source attribution from approved search results so the terminal client can present evidence with an answer.

**Architecture:** web_search emits normalized source metadata beside bounded text output. The agent event path retains the metadata without treating source text as policy.

**Tech Stack:** TypeScript, Node.js standard library, Zod where external data is parsed, Vitest.

**Spec:** docs/superpowers/specs/M7.2-web-aware-answer-evidence-design.md

## Global Constraints

- No new runtime dependency without owner approval.
- Keep provider, tool, and CLI boundaries intact.
- Automated tests remain offline and deterministic.
- Do not publish, upload, or contact a live provider during verification.

## Tasks

1. Add metadata regression tests. 2. Preserve sources in tool result metadata. 3. Render citations where answer UI supports them. 4. Run full offline validation and document limits.

## Verification

Run 
pm.cmd test, 
pm.cmd run build, and git diff --check. Record any live-release action separately because it requires explicit owner approval.